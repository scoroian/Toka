# Prompts de ejecución — specs 2026-04-21

Diez prompts autónomos, uno por spec. Cada uno se pasa tal cual a una nueva sesión de Claude Code dentro del repo `c:\Users\sebas\OneDrive\Escritorio\Proyectos\Toka`.

**Recordatorio común sobre imágenes:** cualquier captura `.png` del emulador (`adb exec-out screencap`), golden o fallo de UI debe redimensionarse a **máximo 1900px de lado mayor** antes de leerla con la tool `Read` — si no, la API rechaza con *"An image in the conversation exceeds the dimension limit for many-image requests (2000px)"* en sesiones multi-imagen. Comprobar dimensiones con `magick identify -format "%wx%h" <ruta>`; redimensionar con `magick <src> -resize 1900x1900\> <dst>` a la misma carpeta (sufijo `_1900px`) o a `c:\tmp\`.

---

## Prompt 1 — Free plan limits + tarea puntual + gating de valoraciones

```
Implementa la spec docs/superpowers/specs/2026-04-21-free-plan-limits-enforcement-design.md en el proyecto Toka (Flutter + Firebase).

Alcance resumido:
1. Añade RecurrenceRule.oneTime al sealed class, serializa correctamente y actualiza task_assignment_helpers (Dart y TS).
2. Crea functions/src/shared/free_limits.ts y lib/core/constants/free_limits.dart con los números de 6.1 (3 miembros, 4 tareas activas, 1 admin, 3 recurrentes).
3. Amplía createTask/updateTask, addMember, grantAdmin y submitReview en functions/src/ para rechazar si el hogar es Free y se excede el límite. Usa isPremium() centralizado.
4. Endurece firestore.rules para reviews: verificar premiumFlags.canUseReviews en el dashboard del hogar.
5. Cliente: añade chip "Puntual" en recurrence_form, banner de límite alcanzado en create_edit_task_screen, deshabilita Invitar/grantAdmin/Valorar en UI cuando no hay plan. Usa nuevas claves ARB.
6. Amplía dashboard.planCounters con contadores en update_dashboard.ts y en HomeDashboard.

Flujo de verificación (sigue CLAUDE.md del proyecto):
- `flutter analyze` debe pasar limpio.
- `dart run build_runner build --delete-conflicting-outputs` para freezed/riverpod.
- `flutter test test/unit/features/tasks/` y `test/unit/features/homes/`.
- `npm --prefix functions test` (casos nuevos en task_limits.test.ts y en submit_review.test.ts).
- Prueba visual en emulador-5554: login con toka.qa.owner (ver CLAUDE.md para coordenadas), hogar Free con 3 miembros → intenta invitar al 4º y confirma banner "plan Free permite hasta 3 miembros". Crea 4 tareas → la 5ª muestra bloqueo. Crea 3 recurrentes + 1 puntual OK, 4ª recurrente bloqueada. Abre sheet de Valorar → debe mostrar mensaje "Valoraciones en Premium".
- Capturas con `adb exec-out screencap -p > c:/tmp/screen.png`. Si supera 1900px de lado mayor, redimensiona antes de leerla con Read (`magick c:/tmp/screen.png -resize 1900x1900\> c:/tmp/screen_1900px.png`).

Criterio de aceptación: los 4 bloqueos visibles + tarea puntual creada + flutter analyze verde + tests verdes. Imprime al final el bloque "## Pruebas manuales requeridas" como pide CLAUDE.md.
```

---

## Prompt 2 — Debug premium gate

```
Implementa la spec docs/superpowers/specs/2026-04-21-debug-premium-gate-design.md en el proyecto Toka.

Alcance:
1. En home_settings_view_model.dart, cambia showDebugPremiumToggle = isOwner && (kDebugMode || remoteConfig.debugToolsEnabled). Añade la key debugToolsEnabled al provider de Remote Config con default false.
2. En HomesRepositoryImpl.debugSetPremiumStatus añade guard `if (kReleaseMode) throw StateError(...)`.
3. En functions/src/homes/index.ts, al inicio de debugSetPremiumStatus, rechaza con permission-denied si process.env.TOKA_ENV === "prod".
4. En functions/src/index.ts, export condicional de debugSetPremiumStatus sólo si TOKA_ENV !== "prod".

Verificación:
- `flutter analyze` limpio.
- `flutter test test/unit/features/homes/home_settings_view_model_test.dart` con el nuevo caso.
- `npm --prefix functions test` — caso que simula TOKA_ENV=prod y verifica permission-denied.
- Build release local: `flutter build apk --release` y confirma con `adb install` que el tile "🧪 DEBUG: Estado premium" ya no aparece en Ajustes del hogar como owner.
- Build debug (`flutter run -d emulator-5554`): el tile SÍ aparece. Captura con `adb exec-out screencap -p > c:/tmp/screen.png`; si supera 1900px redimensiona con `magick` antes de leerla.

Criterio de aceptación: tile invisible en release, visible en debug, callable rechaza en prod simulado, tests verdes.
```

---

## Prompt 3 — FCM permission (Android 13+)

```
Implementa la spec docs/superpowers/specs/2026-04-21-fcm-notifications-permission-design.md en el proyecto Toka.

Alcance:
1. Añade <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> al AndroidManifest.xml.
2. Amplía fcm_token_service.dart con requestPermission() y currentStatus() (wrap de FirebaseMessaging).
3. Crea lib/features/onboarding/presentation/notification_rationale_screen.dart con la rationale + 2 CTA. Persiste notif_rationale_shown_v1 en SharedPreferences.
4. Inserta la pantalla al final del onboarding si currentStatus()==notDetermined o no se ha mostrado.
5. Registra WidgetsBindingObserver en app.dart para sincronizar users/{uid}.notificationPrefs.systemAuthorized al resumed.
6. Amplía notification_settings_view_model.dart con systemAuthorized y banner "Notificaciones bloqueadas" + botón "Abrir ajustes" (usa AppSettings.openNotificationSettings).
7. Añade claves ARB.

Verificación:
- `flutter analyze` limpio.
- `flutter test test/unit/features/notifications/` con los tests nuevos.
- Emulador-5554 con API 33+: primer login con toka.qa.owner, al terminar onboarding debe aparecer pantalla rationale. Tras pulsar "Activar", debe salir el diálogo nativo del sistema. Rechaza → Ajustes → Notificaciones muestra banner rojo "Notificaciones bloqueadas".
- Tras tap "Abrir ajustes" se abre la pantalla de ajustes de app. Vuelve a la app, concede, vuelve: el banner desaparece.
- Captura con `adb exec-out screencap -p > c:/tmp/screen.png`. Redimensionar con `magick` a ≤1900px de lado mayor antes de Read.

Criterio de aceptación: permission prompt aparece, banner reacciona al estado del sistema, tests verdes.
```

---

## Prompt 4 — Ad banner + teclado + padding inferior

```
Implementa la spec docs/superpowers/specs/2026-04-21-ad-banner-keyboard-and-list-bottom-design.md en el proyecto Toka.

Alcance:
1. Añade dep `flutter_keyboard_visibility` a pubspec.yaml si no está.
2. Crea lib/shared/widgets/keyboard_visible_provider.dart y un KeyboardVisibilityBuilder envolviendo MaterialApp en app.dart que alimenta el provider.
3. En lib/shared/widgets/ad_banner.dart, al inicio del build: `if (ref.watch(keyboardVisibleProvider)) return SizedBox.shrink();` (sin disposear el BannerAd).
4. Crea lib/shared/widgets/ad_aware_bottom_padding.dart con la fórmula banner + navBar + navBarGap + safeArea + extra.
5. Aplica padding en today_screen_v2, all_tasks_screen_v2, members_screen y pantalla de historial.

Verificación:
- `flutter analyze` limpio, `flutter test test/ui/features/tasks/` con el golden `today_screen_v2_with_keyboard_test`.
- Emulador-5554: login toka.qa.owner, hogar Premium (sin banner: confirma que padding se ajusta); hogar Free (banner visible). Abre tarea, tap en descripción para abrir teclado → banner debe desaparecer, al cerrar teclado vuelve. En Hoy con 10+ tareas scroll al final: la última debe verse completa por encima de NavBar + banner.
- Captura `adb exec-out screencap -p > c:/tmp/screen.png` y redimensiona con `magick ... -resize 1900x1900\> ...` antes de Read.

Criterio de aceptación: banner oculto con teclado, último card siempre visible, tests verdes.
```

---

## Prompt 5 — Subscription screen rebuild + notif toggles

```
Implementa la spec docs/superpowers/specs/2026-04-21-subscription-screen-rebuild-design.md en el proyecto Toka.

Alcance:
1. Crea subscription_dashboard_provider.dart como Stream sobre homes/{homeId}/views/dashboard.
2. Define SubscriptionDashboard (freezed) con los campos necesarios (status, endsAt, restoreUntil, autoRenew, currentPayerUid, planCounters).
3. Reescribe subscription_management_screen.dart para consumir el stream y renderizar según la tabla de estados de la spec. Crea PlanSummaryCard en widgets/.
4. Convierte notification_settings_view_model a @riverpod Stream sobre users/{uid} para arreglar BUG-13.
5. Reemplaza FutureBuilder por AsyncValue en la pantalla de notificaciones.

Verificación:
- `flutter analyze` limpio; `dart run build_runner build --delete-conflicting-outputs`.
- `flutter test test/unit/features/subscription/` y `test/unit/features/notifications/`.
- `flutter test test/ui/features/subscription/plan_summary_card_test.dart` (6 goldens, uno por estado).
- Emulador-5554: con el toggle debug (dev build) cambia al owner entre los 6 estados y verifica que la pantalla Suscripción se refresca SIN pull-to-refresh. En Premium, los toggles de notificaciones aparecen habilitados al primer frame (no deshabilitados→habilitados).
- Capturas de cada estado. Redimensionar a ≤1900px antes de Read (`magick ... -resize 1900x1900\>`).

Criterio de aceptación: pantalla con contenido en todos los estados, refresco automático, toggles estables.
```

---

## Prompt 6 — Historial: UIDs resueltos + notas visibles

```
Implementa la spec docs/superpowers/specs/2026-04-21-history-and-review-visibility-design.md en el proyecto Toka.

Alcance:
1. En history_event_tile.dart, reemplaza todas las interpolaciones `${event.payload['xxxUid']}` por helper `_nameOrFallback(uid, ref)` que usa MemberDisplayCache. Cubre taskPassed, taskReassigned, memberLeft, memberKicked, adminGranted, adminRevoked.
2. Crea lib/features/history/presentation/history_event_detail_screen.dart y su provider (Stream sobre doc + subcolección reviews).
3. Navegación: tile del historial se vuelve tapable para tipos con reviews. Ruta `/history/:homeId/:eventId`.
4. En la pantalla de detalle, el bloque review muestra estrellas+tags siempre; la nota sólo si authUid == reviewerUid OR authUid == performerUid del evento.
5. Añade sección "Últimas valoraciones" al perfil del miembro usando el mismo filtro.
6. Verifica/actualiza firestore.rules para lectura de reviews según los 2 UIDs permitidos.
7. Añade claves ARB.

Verificación:
- `flutter analyze` limpio; `flutter test test/unit/features/history/` y `test/ui/features/history/`.
- Emulador-5554: login toka.qa.member, hogar Premium, completa una tarea. Cambia a owner, valora con nota, logout. Login admin: no ve la nota. Login owner (reviewer): ve la nota. Login member (performer): ve la nota.
- Pase de turno: crea evento, confirma que el tile muestra alias resuelto y NO el UID crudo.
- Capturas del detalle de evento con las 3 vistas (reviewer/performer/tercero). Redimensionar a ≤1900px antes de Read.

Criterio de aceptación: cero UIDs crudos en historial; nota visible sólo a 2 UIDs; tests verdes.
```

---

## Prompt 7 — Navegación: eliminar duplicados

```
Implementa la spec docs/superpowers/specs/2026-04-21-navigation-consolidation-design.md en el proyecto Toka.

Alcance:
1. Elimina tile "Código de invitación" de settings_screen.dart y home_settings_screen.dart. En home_settings_screen añade tile "Gestionar miembros" que abre MembersScreen.
2. Elimina tile "Gestionar suscripción" de home_settings_screen.dart. Si isPayer, añade info tile "Tu cuenta paga este hogar → Gestionar en Ajustes".
3. Elimina tile "Miembros" y "Cambiar de hogar" duplicados (home_settings y settings).
4. En el flujo "Salir del hogar" de home_settings_screen.dart, navega a `/homes` ANTES de esperar al callable.
5. Audita los routers de feature y elimina rutas nombradas sin referencias.

Verificación:
- `flutter analyze` limpio; `flutter test test/ui/features/homes/home_settings_screen_test.dart` y `test/ui/features/settings/settings_screen_test.dart` (regenerar goldens si es necesario con `--update-goldens`).
- `flutter test test/ui/features/homes/leave_home_navigation_test.dart` (nuevo).
- Emulador-5554: repasa los 3 puntos de entrada del código de invitación — sólo debe quedar en Miembros→FAB. Confirma que suscripción sólo aparece en Ajustes. Salir de hogar desde home_settings: no debe quedar pantalla en blanco, va directo al selector.
- Capturas de Ajustes y Ajustes del hogar antes/después. Redimensionar a ≤1900px.

Criterio de aceptación: ninguna acción duplicada; salir de hogar sin pantalla zombie; tests verdes.
```

---

## Prompt 8 — i18n fechas y plurales ICU

```
Implementa la spec docs/superpowers/specs/2026-04-21-i18n-dates-and-plurals-design.md en el proyecto Toka.

Alcance:
1. Crea lib/core/utils/toka_dates.dart con los 6 helpers (timeShort, dateMediumWithWeekday, dateLongDayMonth, dateLongFull, dateShort, dateTimeShort) basados en intl y locale explícito.
2. Audita `rg 'DateFormat' lib/features` y migra todos los usos a TokaDates pasando `Localizations.localeOf(context)`.
3. Añade claves ARB con sintaxis ICU plural: daysUntil, tasksActiveCount, membersCount, daysLeft, reviewsCount (para es, en, ro — ro necesita cláusula `few` para 2-19).
4. Elimina condicionales manuales `if (n == 1) "día" else "días"` y usa las claves plural.
5. Verifica que app.dart incluye GlobalMaterialLocalizations/GlobalWidgetsLocalizations/GlobalCupertinoLocalizations delegates.

Verificación:
- `flutter gen-l10n` sin errores; `flutter analyze` limpio.
- `flutter test test/unit/core/utils/toka_dates_test.dart` (3 locales x 3 fechas x 6 helpers).
- Regenera goldens tocados con `flutter test --update-goldens`.
- Emulador-5554: cambia idioma a en y ro en Ajustes → Idioma. Crea tarea anual; abre detalle y preview: meses traducidos, 24h, fechas con formato local.
- Capturas en 3 locales. Redimensionar a ≤1900px con magick.

Criterio de aceptación: cero meses en inglés cuando locale==es/ro, plurales correctos, tests verdes.
```

---

## Prompt 9 — Rescue UX + expiredFree messaging

```
Implementa la spec docs/superpowers/specs/2026-04-21-rescue-and-expiredfree-ux-design.md en el proyecto Toka.

Alcance:
1. Cambia cálculo de daysLeft a `ceil` tanto en backend (functions/src/jobs/premium_daily.ts) como en cliente (helper daysLeftFrom en lib/features/subscription/application/).
2. Crea lib/features/subscription/presentation/widgets/premium_state_banner.dart con render por estado según la tabla de la spec (rescue ≥1 día, rescue <1 día, cancelledPendingEnd, expiredFree, restorable).
3. Inserta el banner como primer child en today_screen_v2 y en home_settings_screen.
4. Ajusta rescue_screen.dart: cuenta atrás precisa en horas si queda <1 día; muestra lastBillingError si existe.
5. Paywall recibe PaywallEntryContext enum (fromFree/fromExpired/fromRescue/fromRestorable) y cambia título+CTA+subtítulo.
6. Claves ARB con placeholder {date} y {days}.

Verificación:
- `flutter analyze` limpio; `dart run build_runner build`.
- `flutter test test/unit/features/subscription/days_left_test.dart` y `test/ui/features/subscription/premium_state_banner_test.dart` (golden por estado).
- Emulador-5554: con toggle debug recorre los 6 estados y confirma visualmente cada banner. En `rescue` con premiumEndsAt a 7h, el banner debe decir "Quedan 7 horas". En `expiredFree`, el paywall abre con título "Reactivar Premium" y no "Hazte Premium".
- Capturas de cada estado. Redimensionar a ≤1900px.

Criterio de aceptación: banners correctos por estado, ceil aplicado, copy diferenciada, tests verdes.
```

---

## Prompt 10 — Task form + rotación + detalles de lista

```
Implementa la spec docs/superpowers/specs/2026-04-21-task-form-and-list-fixes-design.md en el proyecto Toka.

Orden recomendado: BUG-07 → BUG-06 → BUG-08 → BUG-23 → BUG-01.

Alcance:
1. (BUG-07) Añade RecurrenceNotifier.hydrateFrom(RecurrenceRule) que mapea las 8 variantes (incl. oneTime si spec 1 ya está) al estado interno. Llámalo desde create_edit_task_view_model al cargar tarea existente.
2. (BUG-06) En functions/src/tasks/pass_task_turn.ts, separa la lógica de passTaskTurn (siempre avanza en assignmentOrder) del cron de miss (que sí consulta onMissAssign). Ajusta pass_turn_helpers.ts y tests.
3. (BUG-08) En task_detail_screen_v2.dart, _onDelete navega con GoRouter.pop() ANTES de awaitear el callable. Snackbar de error si falla.
4. (BUG-23) En upcoming_dates_preview.dart, si la recurrencia es YearlyFixedRule/YearlyNthRule, usa TokaDates.dateLongFull (con año). Igual en tile de Hoy para anuales a >30 días.
5. (BUG-01) En el login SignInForm, TextField de email: autocorrect:false, enableSuggestions:false, textCapitalization:none, autofillHints:[AutofillHints.email]. Replicar en todo campo email de la app.

Verificación:
- `flutter analyze` limpio; `dart run build_runner build`.
- `flutter test test/unit/features/tasks/` y `test/ui/features/tasks/`.
- `npm --prefix functions test` — pass_turn_helpers y pass_task_turn con caso 2 miembros + sameAssignee → debe alternar A↔B.
- Emulador-5554: (7) edita una tarea recurrente existente → el formulario muestra la recurrencia actual preseleccionada. (6) crea hogar con 2 miembros y onMissAssign=sameAssignee; pasa turno → asignado cambia. (8) borra tarea desde detalle → navega a lista sin quedarse colgado. (23) crea tarea anual; preview muestra "25 de abril de 2027".
- (1) con teclado MIUI o similar activo, escribe email rápido → no hay reemplazos automáticos.
- Capturas para cada bug. Redimensionar a ≤1900px antes de Read.

Criterio de aceptación: los 5 bugs cerrados visualmente + tests verdes.
```
