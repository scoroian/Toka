# Spec-01: i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the complete i18n system: ARB strings, code generation, Language model/repository, LocaleService, Riverpod providers, and a reusable LanguageSelectorWidget that drives `MaterialApp.locale`.

**Architecture:** The `Language` model (freezed) is fetched from Firestore's `languages/` top-level collection via a repository interface + impl. A `LocaleService` persists locale to SharedPreferences (pre-auth) or Firestore `users/{uid}.locale` (post-auth). A keepAlive `LocaleNotifier` holds the active `Locale` and is read by `app.dart` to drive `MaterialApp.router.locale`.

**Tech Stack:** Dart/Flutter, freezed, riverpod_annotation, cloud_firestore, shared_preferences, flutter_localizations, fake_cloud_firestore (integration tests), mocktail (unit mocks)

---

## File Map

**Create:**
- `lib/features/i18n/domain/language.dart` — freezed Language model + fromFirestore factory
- `lib/features/i18n/domain/language_repository.dart` — abstract interface
- `lib/features/i18n/data/language_repository_impl.dart` — Firestore impl
- `lib/core/services/locale_service.dart` — SharedPreferences + Firestore locale persistence
- `lib/features/i18n/application/language_provider.dart` — availableLanguagesProvider
- `lib/features/i18n/application/locale_provider.dart` — LocaleNotifier (keepAlive)
- `lib/features/i18n/presentation/language_selector_widget.dart` — reusable list widget
- `scripts/seed_languages.ts` — emulator seed data
- `test/unit/features/i18n/language_test.dart`
- `test/unit/features/i18n/locale_service_test.dart`
- `test/unit/features/i18n/language_repository_impl_test.dart`
- `test/integration/features/i18n/language_fetch_test.dart`
- `test/ui/features/i18n/language_selector_widget_test.dart`

**Modify:**
- `lib/l10n/app_es.arb` — add auth/onboarding/settings/language keys
- `lib/l10n/app_en.arb` — add same keys in English
- `lib/l10n/app_ro.arb` — add same keys in Romanian
- `lib/l10n/app_localizations.dart` — regenerated (flutter gen-l10n)
- `lib/l10n/app_localizations_es.dart` — regenerated
- `lib/l10n/app_localizations_en.dart` — regenerated
- `lib/l10n/app_localizations_ro.dart` — regenerated
- `lib/core/errors/exceptions.dart` — add LanguagesFetchException
- `lib/app.dart` — connect locale from localeNotifierProvider

---

### Task 1: Expand ARB files

**Files:**
- Modify: `lib/l10n/app_es.arb`
- Modify: `lib/l10n/app_en.arb`
- Modify: `lib/l10n/app_ro.arb`

- [ ] **Step 1: Update app_es.arb**

Replace full file content:

```json
{
  "@@locale": "es",
  "appName": "Toka",
  "@appName": { "description": "App name" },
  "loading": "Cargando...",
  "@loading": { "description": "Generic loading message" },
  "error_generic": "Algo salió mal. Inténtalo de nuevo.",
  "@error_generic": { "description": "Generic error message" },
  "retry": "Reintentar",
  "@retry": { "description": "Retry button label" },
  "cancel": "Cancelar",
  "@cancel": { "description": "Cancel button label" },
  "confirm": "Confirmar",
  "@confirm": { "description": "Confirm button label" },
  "save": "Guardar",
  "@save": { "description": "Save button label" },
  "delete": "Eliminar",
  "@delete": { "description": "Delete button label" },
  "back": "Atrás",
  "@back": { "description": "Back button label" },
  "next": "Siguiente",
  "@next": { "description": "Next button label" },
  "done": "Hecho",
  "@done": { "description": "Done button label" },
  "skip": "Omitir",
  "@skip": { "description": "Skip button label" },
  "auth_title": "Bienvenido a Toka",
  "@auth_title": { "description": "Auth screen title" },
  "auth_subtitle": "Gestiona las tareas del hogar juntos",
  "@auth_subtitle": { "description": "Auth screen subtitle" },
  "auth_google": "Continuar con Google",
  "@auth_google": { "description": "Google sign-in button" },
  "auth_apple": "Continuar con Apple",
  "@auth_apple": { "description": "Apple sign-in button" },
  "auth_email": "Continuar con email",
  "@auth_email": { "description": "Email sign-in button" },
  "auth_email_label": "Correo electrónico",
  "@auth_email_label": { "description": "Email field label" },
  "auth_password_label": "Contraseña",
  "@auth_password_label": { "description": "Password field label" },
  "auth_login": "Iniciar sesión",
  "@auth_login": { "description": "Login button" },
  "auth_register": "Crear cuenta",
  "@auth_register": { "description": "Register button" },
  "auth_forgot_password": "¿Olvidaste tu contraseña?",
  "@auth_forgot_password": { "description": "Forgot password link" },
  "auth_reset_sent": "Te hemos enviado un correo para restablecer tu contraseña",
  "@auth_reset_sent": { "description": "Password reset email sent message" },
  "onboarding_welcome": "Bienvenido",
  "@onboarding_welcome": { "description": "Onboarding welcome title" },
  "onboarding_select_language": "Elige tu idioma",
  "@onboarding_select_language": { "description": "Onboarding language selection step" },
  "onboarding_create_home": "Crear un hogar",
  "@onboarding_create_home": { "description": "Onboarding create home option" },
  "onboarding_join_home": "Unirme a un hogar",
  "@onboarding_join_home": { "description": "Onboarding join home option" },
  "onboarding_your_name": "¿Cómo te llamas?",
  "@onboarding_your_name": { "description": "Onboarding name step" },
  "onboarding_photo_optional": "Añadir foto (opcional)",
  "@onboarding_photo_optional": { "description": "Onboarding photo step" },
  "settings_title": "Ajustes",
  "@settings_title": { "description": "Settings screen title" },
  "settings_language": "Idioma",
  "@settings_language": { "description": "Settings language option" },
  "settings_account": "Cuenta",
  "@settings_account": { "description": "Settings account option" },
  "settings_privacy": "Privacidad",
  "@settings_privacy": { "description": "Settings privacy option" },
  "settings_notifications": "Notificaciones",
  "@settings_notifications": { "description": "Settings notifications option" },
  "settings_subscription": "Suscripción",
  "@settings_subscription": { "description": "Settings subscription option" },
  "settings_logout": "Cerrar sesión",
  "@settings_logout": { "description": "Settings logout option" },
  "language_select_title": "Seleccionar idioma",
  "@language_select_title": { "description": "Language selection screen title" },
  "language_select_subtitle": "Elige el idioma de la aplicación",
  "@language_select_subtitle": { "description": "Language selection screen subtitle" },
  "language_saved": "Idioma guardado",
  "@language_saved": { "description": "Confirmation message after saving language" }
}
```

- [ ] **Step 2: Update app_en.arb**

Replace full file content:

```json
{
  "@@locale": "en",
  "appName": "Toka",
  "@appName": { "description": "App name" },
  "loading": "Loading...",
  "@loading": { "description": "Generic loading message" },
  "error_generic": "Something went wrong. Please try again.",
  "@error_generic": { "description": "Generic error message" },
  "retry": "Retry",
  "@retry": { "description": "Retry button label" },
  "cancel": "Cancel",
  "@cancel": { "description": "Cancel button label" },
  "confirm": "Confirm",
  "@confirm": { "description": "Confirm button label" },
  "save": "Save",
  "@save": { "description": "Save button label" },
  "delete": "Delete",
  "@delete": { "description": "Delete button label" },
  "back": "Back",
  "@back": { "description": "Back button label" },
  "next": "Next",
  "@next": { "description": "Next button label" },
  "done": "Done",
  "@done": { "description": "Done button label" },
  "skip": "Skip",
  "@skip": { "description": "Skip button label" },
  "auth_title": "Welcome to Toka",
  "@auth_title": { "description": "Auth screen title" },
  "auth_subtitle": "Manage household tasks together",
  "@auth_subtitle": { "description": "Auth screen subtitle" },
  "auth_google": "Continue with Google",
  "@auth_google": { "description": "Google sign-in button" },
  "auth_apple": "Continue with Apple",
  "@auth_apple": { "description": "Apple sign-in button" },
  "auth_email": "Continue with email",
  "@auth_email": { "description": "Email sign-in button" },
  "auth_email_label": "Email address",
  "@auth_email_label": { "description": "Email field label" },
  "auth_password_label": "Password",
  "@auth_password_label": { "description": "Password field label" },
  "auth_login": "Sign in",
  "@auth_login": { "description": "Login button" },
  "auth_register": "Create account",
  "@auth_register": { "description": "Register button" },
  "auth_forgot_password": "Forgot your password?",
  "@auth_forgot_password": { "description": "Forgot password link" },
  "auth_reset_sent": "We have sent you an email to reset your password",
  "@auth_reset_sent": { "description": "Password reset email sent message" },
  "onboarding_welcome": "Welcome",
  "@onboarding_welcome": { "description": "Onboarding welcome title" },
  "onboarding_select_language": "Choose your language",
  "@onboarding_select_language": { "description": "Onboarding language selection step" },
  "onboarding_create_home": "Create a home",
  "@onboarding_create_home": { "description": "Onboarding create home option" },
  "onboarding_join_home": "Join a home",
  "@onboarding_join_home": { "description": "Onboarding join home option" },
  "onboarding_your_name": "What is your name?",
  "@onboarding_your_name": { "description": "Onboarding name step" },
  "onboarding_photo_optional": "Add a photo (optional)",
  "@onboarding_photo_optional": { "description": "Onboarding photo step" },
  "settings_title": "Settings",
  "@settings_title": { "description": "Settings screen title" },
  "settings_language": "Language",
  "@settings_language": { "description": "Settings language option" },
  "settings_account": "Account",
  "@settings_account": { "description": "Settings account option" },
  "settings_privacy": "Privacy",
  "@settings_privacy": { "description": "Settings privacy option" },
  "settings_notifications": "Notifications",
  "@settings_notifications": { "description": "Settings notifications option" },
  "settings_subscription": "Subscription",
  "@settings_subscription": { "description": "Settings subscription option" },
  "settings_logout": "Sign out",
  "@settings_logout": { "description": "Settings logout option" },
  "language_select_title": "Select language",
  "@language_select_title": { "description": "Language selection screen title" },
  "language_select_subtitle": "Choose the app language",
  "@language_select_subtitle": { "description": "Language selection screen subtitle" },
  "language_saved": "Language saved",
  "@language_saved": { "description": "Confirmation message after saving language" }
}
```

- [ ] **Step 3: Update app_ro.arb**

Replace full file content:

```json
{
  "@@locale": "ro",
  "appName": "Toka",
  "@appName": { "description": "App name" },
  "loading": "Se încarcă...",
  "@loading": { "description": "Generic loading message" },
  "error_generic": "Ceva a mers greșit. Încearcă din nou.",
  "@error_generic": { "description": "Generic error message" },
  "retry": "Reîncearcă",
  "@retry": { "description": "Retry button label" },
  "cancel": "Anulează",
  "@cancel": { "description": "Cancel button label" },
  "confirm": "Confirmă",
  "@confirm": { "description": "Confirm button label" },
  "save": "Salvează",
  "@save": { "description": "Save button label" },
  "delete": "Șterge",
  "@delete": { "description": "Delete button label" },
  "back": "Înapoi",
  "@back": { "description": "Back button label" },
  "next": "Următor",
  "@next": { "description": "Next button label" },
  "done": "Gata",
  "@done": { "description": "Done button label" },
  "skip": "Sari",
  "@skip": { "description": "Skip button label" },
  "auth_title": "Bun venit la Toka",
  "@auth_title": { "description": "Auth screen title" },
  "auth_subtitle": "Gestionați sarcinile gospodăriei împreună",
  "@auth_subtitle": { "description": "Auth screen subtitle" },
  "auth_google": "Continuați cu Google",
  "@auth_google": { "description": "Google sign-in button" },
  "auth_apple": "Continuați cu Apple",
  "@auth_apple": { "description": "Apple sign-in button" },
  "auth_email": "Continuați cu email",
  "@auth_email": { "description": "Email sign-in button" },
  "auth_email_label": "Adresă de email",
  "@auth_email_label": { "description": "Email field label" },
  "auth_password_label": "Parolă",
  "@auth_password_label": { "description": "Password field label" },
  "auth_login": "Autentificare",
  "@auth_login": { "description": "Login button" },
  "auth_register": "Creați cont",
  "@auth_register": { "description": "Register button" },
  "auth_forgot_password": "Ați uitat parola?",
  "@auth_forgot_password": { "description": "Forgot password link" },
  "auth_reset_sent": "V-am trimis un email pentru a vă reseta parola",
  "@auth_reset_sent": { "description": "Password reset email sent message" },
  "onboarding_welcome": "Bun venit",
  "@onboarding_welcome": { "description": "Onboarding welcome title" },
  "onboarding_select_language": "Alegeți limba",
  "@onboarding_select_language": { "description": "Onboarding language selection step" },
  "onboarding_create_home": "Creați o locuință",
  "@onboarding_create_home": { "description": "Onboarding create home option" },
  "onboarding_join_home": "Alăturați-vă unei locuințe",
  "@onboarding_join_home": { "description": "Onboarding join home option" },
  "onboarding_your_name": "Cum vă numiți?",
  "@onboarding_your_name": { "description": "Onboarding name step" },
  "onboarding_photo_optional": "Adăugați o fotografie (opțional)",
  "@onboarding_photo_optional": { "description": "Onboarding photo step" },
  "settings_title": "Setări",
  "@settings_title": { "description": "Settings screen title" },
  "settings_language": "Limbă",
  "@settings_language": { "description": "Settings language option" },
  "settings_account": "Cont",
  "@settings_account": { "description": "Settings account option" },
  "settings_privacy": "Confidențialitate",
  "@settings_privacy": { "description": "Settings privacy option" },
  "settings_notifications": "Notificări",
  "@settings_notifications": { "description": "Settings notifications option" },
  "settings_subscription": "Abonament",
  "@settings_subscription": { "description": "Settings subscription option" },
  "settings_logout": "Deconectare",
  "@settings_logout": { "description": "Settings logout option" },
  "language_select_title": "Selectați limba",
  "@language_select_title": { "description": "Language selection screen title" },
  "language_select_subtitle": "Alegeți limba aplicației",
  "@language_select_subtitle": { "description": "Language selection screen subtitle" },
  "language_saved": "Limbă salvată",
  "@language_saved": { "description": "Confirmation message after saving language" }
}
```

- [ ] **Step 4: Run flutter gen-l10n**

```bash
cd "c:/Users/sebas/OneDrive/Escritorio/Proyectos/Toka" && flutter gen-l10n
```

Expected: Exit 0, no errors. Files `lib/l10n/app_localizations*.dart` are updated.

- [ ] **Step 5: Commit**

```bash
git add lib/l10n/
git commit -m "feat(i18n): expand ARB files with auth/onboarding/settings/language keys"
```

---

### Task 2: Add LanguagesFetchException

**Files:**
- Modify: `lib/core/errors/exceptions.dart`

- [ ] **Step 1: Write failing test**

File: `test/unit/core/errors/exceptions_test.dart` — add at the end of `main()`:

```dart
group('LanguagesFetchException', () {
  test('has default message', () {
    const e = LanguagesFetchException();
    expect(e.message, equals('Failed to fetch languages'));
  });
  test('toString includes message', () {
    const e = LanguagesFetchException('network error');
    expect(e.toString(), equals('LanguagesFetchException: network error'));
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
flutter test test/unit/core/errors/exceptions_test.dart
```

Expected: FAIL — `LanguagesFetchException` not found.

- [ ] **Step 3: Add exception to exceptions.dart**

Append to `lib/core/errors/exceptions.dart`:

```dart

class LanguagesFetchException implements Exception {
  const LanguagesFetchException([this.message = 'Failed to fetch languages']);
  final String message;
  @override
  String toString() => 'LanguagesFetchException: $message';
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
flutter test test/unit/core/errors/exceptions_test.dart
```

Expected: All tests PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/core/errors/exceptions.dart test/unit/core/errors/exceptions_test.dart
git commit -m "feat(i18n): add LanguagesFetchException"
```

---

### Task 3: Language domain model + LanguageRepository interface

**Files:**
- Create: `lib/features/i18n/domain/language.dart`
- Create: `lib/features/i18n/domain/language_repository.dart`

- [ ] **Step 1: Write failing unit tests for Language model**

Create file `test/unit/features/i18n/language_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:toka/features/i18n/domain/language.dart';

void main() {
  group('Language.fromFirestore', () {
    test('parses a full valid map', () {
      final data = {
        'code': 'es',
        'name': 'Español',
        'flag': '🇪🇸',
        'arb_key': 'app_es',
        'enabled': true,
        'sort_order': 1,
      };
      final lang = Language.fromFirestore(data);
      expect(lang.code, 'es');
      expect(lang.name, 'Español');
      expect(lang.flag, '🇪🇸');
      expect(lang.arbKey, 'app_es');
      expect(lang.enabled, true);
      expect(lang.sortOrder, 1);
    });

    test('uses default values when optional fields are absent', () {
      final data = {
        'code': 'en',
        'name': 'English',
        'flag': '🇬🇧',
        'arb_key': 'app_en',
      };
      final lang = Language.fromFirestore(data);
      expect(lang.enabled, true);
      expect(lang.sortOrder, 99);
    });
  });
}
```

- [ ] **Step 2: Run test to verify it fails**

```bash
flutter test test/unit/features/i18n/language_test.dart
```

Expected: FAIL — `Language` not found.

- [ ] **Step 3: Create language.dart**

Create `lib/features/i18n/domain/language.dart`:

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'language.freezed.dart';

@freezed
class Language with _$Language {
  const Language._();

  const factory Language({
    required String code,
    required String name,
    required String flag,
    required String arbKey,
    required bool enabled,
    required int sortOrder,
  }) = _Language;

  factory Language.fromFirestore(Map<String, dynamic> data) => Language(
        code: data['code'] as String,
        name: data['name'] as String,
        flag: data['flag'] as String,
        arbKey: data['arb_key'] as String,
        enabled: data['enabled'] as bool? ?? true,
        sortOrder: data['sort_order'] as int? ?? 99,
      );
}
```

- [ ] **Step 4: Create language_repository.dart**

Create `lib/features/i18n/domain/language_repository.dart`:

```dart
import 'language.dart';

abstract interface class LanguageRepository {
  Future<List<Language>> fetchAvailableLanguages();
}
```

- [ ] **Step 5: Run build_runner**

```bash
cd "c:/Users/sebas/OneDrive/Escritorio/Proyectos/Toka" && dart run build_runner build --delete-conflicting-outputs
```

Expected: Exit 0. File `lib/features/i18n/domain/language.freezed.dart` created.

- [ ] **Step 6: Run tests to verify they pass**

```bash
flutter test test/unit/features/i18n/language_test.dart
```

Expected: All 2 tests PASS.

- [ ] **Step 7: Commit**

```bash
git add lib/features/i18n/domain/ test/unit/features/i18n/language_test.dart
git commit -m "feat(i18n): add Language freezed model and LanguageRepository interface"
```

---

### Task 4: LanguageRepositoryImpl

**Files:**
- Create: `lib/features/i18n/data/language_repository_impl.dart`

- [ ] **Step 1: Write failing unit tests**

Create `test/unit/features/i18n/language_repository_impl_test.dart`:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:fake_cloud_firestore/fake_cloud_firestore.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:toka/core/errors/exceptions.dart';
import 'package:toka/features/i18n/data/language_repository_impl.dart';

void main() {
  late FakeFirebaseFirestore fakeFirestore;
  late LanguageRepositoryImpl repo;

  setUp(() {
    fakeFirestore = FakeFirebaseFirestore();
    repo = LanguageRepositoryImpl(firestore: fakeFirestore);
  });

  Future<void> seedLanguages() async {
    final col = fakeFirestore.collection('languages');
    await col.doc('es').set({
      'code': 'es', 'name': 'Español', 'flag': '🇪🇸',
      'arb_key': 'app_es', 'enabled': true, 'sort_order': 1,
    });
    await col.doc('en').set({
      'code': 'en', 'name': 'English', 'flag': '🇬🇧',
      'arb_key': 'app_en', 'enabled': true, 'sort_order': 2,
    });
    await col.doc('ro').set({
      'code': 'ro', 'name': 'Română', 'flag': '🇷🇴',
      'arb_key': 'app_ro', 'enabled': true, 'sort_order': 3,
    });
  }

  test('returns languages ordered by sort_order', () async {
    await seedLanguages();
    final langs = await repo.fetchAvailableLanguages();
    expect(langs.length, 3);
    expect(langs[0].code, 'es');
    expect(langs[1].code, 'en');
    expect(langs[2].code, 'ro');
  });

  test('filters out disabled languages', () async {
    await seedLanguages();
    await fakeFirestore.collection('languages').doc('ro').update({'enabled': false});
    final langs = await repo.fetchAvailableLanguages();
    expect(langs.length, 2);
    expect(langs.any((l) => l.code == 'ro'), isFalse);
  });

  test('throws LanguagesFetchException when Firestore fails', () async {
    // Use a real repo with a bad firestore to simulate failure:
    // We mock by throwing inside — test with a subclass that throws.
    final throwingRepo = _ThrowingRepo();
    expect(
      () => throwingRepo.fetchAvailableLanguages(),
      throwsA(isA<LanguagesFetchException>()),
    );
  });
}

class _ThrowingRepo extends LanguageRepositoryImpl {
  _ThrowingRepo() : super(firestore: FakeFirebaseFirestore());

  @override
  Future fetchAvailableLanguages() async {
    throw const LanguagesFetchException('simulated failure');
  }
}
```

- [ ] **Step 2: Run test to verify it fails**

```bash
flutter test test/unit/features/i18n/language_repository_impl_test.dart
```

Expected: FAIL — `LanguageRepositoryImpl` not found.

- [ ] **Step 3: Create language_repository_impl.dart**

Create `lib/features/i18n/data/language_repository_impl.dart`:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';

import '../../../core/errors/exceptions.dart';
import '../domain/language.dart';
import '../domain/language_repository.dart';

class LanguageRepositoryImpl implements LanguageRepository {
  LanguageRepositoryImpl({required FirebaseFirestore firestore})
      : _firestore = firestore;

  final FirebaseFirestore _firestore;

  @override
  Future<List<Language>> fetchAvailableLanguages() async {
    try {
      final snapshot = await _firestore
          .collection('languages')
          .where('enabled', isEqualTo: true)
          .orderBy('sort_order')
          .get();
      return snapshot.docs
          .map((doc) => Language.fromFirestore(doc.data()))
          .toList();
    } on FirebaseException catch (e) {
      throw LanguagesFetchException(e.message ?? 'Firestore error');
    } catch (e) {
      throw LanguagesFetchException(e.toString());
    }
  }
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
flutter test test/unit/features/i18n/language_repository_impl_test.dart
```

Expected: All 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/features/i18n/data/ test/unit/features/i18n/language_repository_impl_test.dart
git commit -m "feat(i18n): add LanguageRepositoryImpl with Firestore query"
```

---

### Task 5: LocaleService

**Files:**
- Create: `lib/core/services/locale_service.dart`

- [ ] **Step 1: Write failing unit tests**

Create `test/unit/features/i18n/locale_service_test.dart`:

```dart
import 'dart:io';

import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:fake_cloud_firestore/fake_cloud_firestore.dart';
import 'package:flutter/widgets.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:toka/core/services/locale_service.dart';

void main() {
  late FakeFirebaseFirestore fakeFirestore;

  setUp(() {
    fakeFirestore = FakeFirebaseFirestore();
    SharedPreferences.setMockInitialValues({});
  });

  Future<LocaleService> buildService() async {
    final prefs = await SharedPreferences.getInstance();
    return LocaleService(prefs: prefs, firestore: fakeFirestore);
  }

  group('getCurrentLocale', () {
    test('returns SharedPreferences locale when no uid', () async {
      SharedPreferences.setMockInitialValues({'locale': 'en'});
      final service = await buildService();
      final locale = await service.getCurrentLocale(null);
      expect(locale, const Locale('en'));
    });

    test('returns Firestore locale when uid is provided', () async {
      await fakeFirestore
          .collection('users')
          .doc('uid123')
          .set({'locale': 'ro'});
      final service = await buildService();
      final locale = await service.getCurrentLocale('uid123');
      expect(locale, const Locale('ro'));
    });

    test('falls back to SharedPreferences if Firestore has no locale', () async {
      SharedPreferences.setMockInitialValues({'locale': 'en'});
      await fakeFirestore
          .collection('users')
          .doc('uid456')
          .set({'displayName': 'Test'});
      final service = await buildService();
      final locale = await service.getCurrentLocale('uid456');
      expect(locale, const Locale('en'));
    });

    test('falls back to es if no preference and device locale unsupported',
        () async {
      // No prefs, no Firestore, device locale is mocked via unsupported
      final prefs = await SharedPreferences.getInstance();
      final service = LocaleService(
        prefs: prefs,
        firestore: fakeFirestore,
        overrideDeviceLocale: const Locale('de'),
      );
      final locale = await service.getCurrentLocale(null);
      expect(locale, const Locale('es'));
    });

    test('returns device locale when it is supported', () async {
      final prefs = await SharedPreferences.getInstance();
      final service = LocaleService(
        prefs: prefs,
        firestore: fakeFirestore,
        overrideDeviceLocale: const Locale('en'),
      );
      final locale = await service.getCurrentLocale(null);
      expect(locale, const Locale('en'));
    });
  });

  group('saveLocale', () {
    test('always saves to SharedPreferences', () async {
      final service = await buildService();
      await service.saveLocale('ro', null);
      final prefs = await SharedPreferences.getInstance();
      expect(prefs.getString('locale'), 'ro');
    });

    test('also saves to Firestore when uid is provided', () async {
      final service = await buildService();
      await service.saveLocale('en', 'uid789');
      final doc = await fakeFirestore.collection('users').doc('uid789').get();
      expect(doc.data()?['locale'], 'en');
    });
  });
}
```

- [ ] **Step 2: Run test to verify it fails**

```bash
flutter test test/unit/features/i18n/locale_service_test.dart
```

Expected: FAIL — `LocaleService` not found.

- [ ] **Step 3: Create locale_service.dart**

Create `lib/core/services/locale_service.dart`:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter/widgets.dart';
import 'package:shared_preferences/shared_preferences.dart';

class LocaleService {
  LocaleService({
    required SharedPreferences prefs,
    required FirebaseFirestore firestore,
    Locale? overrideDeviceLocale,
  })  : _prefs = prefs,
        _firestore = firestore,
        _overrideDeviceLocale = overrideDeviceLocale;

  static const _key = 'locale';
  static const fallback = Locale('es');
  static const supported = [Locale('es'), Locale('en'), Locale('ro')];

  final SharedPreferences _prefs;
  final FirebaseFirestore _firestore;
  final Locale? _overrideDeviceLocale;

  /// Returns the active locale for the given user.
  /// Priority: Firestore (uid) → SharedPreferences → device locale → fallback.
  Future<Locale> getCurrentLocale(String? uid) async {
    if (uid != null) {
      try {
        final doc =
            await _firestore.collection('users').doc(uid).get();
        final code = doc.data()?['locale'] as String?;
        if (code != null && code.isNotEmpty) {
          return Locale(code);
        }
      } catch (_) {
        // Ignored — fall through to SharedPreferences
      }
    }

    final savedCode = _prefs.getString(_key);
    if (savedCode != null && savedCode.isNotEmpty) {
      return Locale(savedCode);
    }

    final device = _overrideDeviceLocale ?? _resolveDeviceLocale();
    if (supported.contains(device)) {
      return device;
    }
    return fallback;
  }

  /// Saves the locale to SharedPreferences and, if a uid is provided, to Firestore.
  Future<void> saveLocale(String code, String? uid) async {
    await _prefs.setString(_key, code);
    if (uid != null) {
      await _firestore
          .collection('users')
          .doc(uid)
          .set({'locale': code}, SetOptions(merge: true));
    }
  }

  Locale _resolveDeviceLocale() {
    // Platform.localeName returns e.g. "es_ES" or "en_US"
    final name = WidgetsBinding
            .instance.platformDispatcher.locale.languageCode;
    return Locale(name);
  }
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
flutter test test/unit/features/i18n/locale_service_test.dart
```

Expected: All 7 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add lib/core/services/locale_service.dart test/unit/features/i18n/locale_service_test.dart
git commit -m "feat(i18n): add LocaleService with SharedPreferences + Firestore persistence"
```

---

### Task 6: Providers

**Files:**
- Create: `lib/features/i18n/application/language_provider.dart`
- Create: `lib/features/i18n/application/locale_provider.dart`

- [ ] **Step 1: Create language_provider.dart**

Create `lib/features/i18n/application/language_provider.dart`:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';

import '../data/language_repository_impl.dart';
import '../domain/language.dart';
import '../domain/language_repository.dart';

part 'language_provider.g.dart';

@riverpod
LanguageRepository languageRepository(Ref ref) {
  return LanguageRepositoryImpl(firestore: FirebaseFirestore.instance);
}

@riverpod
Future<List<Language>> availableLanguages(Ref ref) {
  return ref.watch(languageRepositoryProvider).fetchAvailableLanguages();
}
```

- [ ] **Step 2: Create locale_provider.dart**

Create `lib/features/i18n/application/locale_provider.dart`:

```dart
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter/widgets.dart';
import 'package:riverpod_annotation/riverpod_annotation.dart';
import 'package:shared_preferences/shared_preferences.dart';

import '../../../core/services/locale_service.dart';

part 'locale_provider.g.dart';

@Riverpod(keepAlive: true)
class LocaleNotifier extends _$LocaleNotifier {
  @override
  Locale build() => LocaleService.fallback;

  Future<void> initialize(String? uid) async {
    final service = await _buildService();
    state = await service.getCurrentLocale(uid);
  }

  Future<void> setLocale(String code, String? uid) async {
    final service = await _buildService();
    await service.saveLocale(code, uid);
    state = Locale(code);
  }

  Future<LocaleService> _buildService() async {
    final prefs = await SharedPreferences.getInstance();
    return LocaleService(
      prefs: prefs,
      firestore: FirebaseFirestore.instance,
    );
  }
}
```

- [ ] **Step 3: Run build_runner**

```bash
cd "c:/Users/sebas/OneDrive/Escritorio/Proyectos/Toka" && dart run build_runner build --delete-conflicting-outputs
```

Expected: Exit 0. Files `language_provider.g.dart` and `locale_provider.g.dart` created.

- [ ] **Step 4: Commit**

```bash
git add lib/features/i18n/application/
git commit -m "feat(i18n): add availableLanguages and LocaleNotifier providers"
```

---

### Task 7: LanguageSelectorWidget

**Files:**
- Create: `lib/features/i18n/presentation/language_selector_widget.dart`

- [ ] **Step 1: Create language_selector_widget.dart**

Create `lib/features/i18n/presentation/language_selector_widget.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../l10n/app_localizations.dart';
import '../application/language_provider.dart';
import '../application/locale_provider.dart';
import '../domain/language.dart';

class LanguageSelectorWidget extends ConsumerWidget {
  const LanguageSelectorWidget({
    super.key,
    this.showTitle = true,
    this.onSelected,
  });

  final bool showTitle;
  final VoidCallback? onSelected;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final l10n = AppLocalizations.of(context);
    final currentLocale = ref.watch(localeNotifierProvider);
    final languagesAsync = ref.watch(availableLanguagesProvider);

    return Column(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        if (showTitle) ...[
          Text(
            l10n.language_select_title,
            style: Theme.of(context).textTheme.titleLarge,
          ),
          const SizedBox(height: 4),
          Text(
            l10n.language_select_subtitle,
            style: Theme.of(context).textTheme.bodyMedium,
          ),
          const SizedBox(height: 16),
        ],
        languagesAsync.when(
          data: (languages) => _LanguageList(
            languages: languages,
            currentLocale: currentLocale,
            onSelected: onSelected,
          ),
          loading: () => const Center(child: CircularProgressIndicator()),
          error: (_, __) => Center(child: Text(l10n.error_generic)),
        ),
      ],
    );
  }
}

class _LanguageList extends ConsumerWidget {
  const _LanguageList({
    required this.languages,
    required this.currentLocale,
    this.onSelected,
  });

  final List<Language> languages;
  final Locale currentLocale;
  final VoidCallback? onSelected;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return ListView.builder(
      shrinkWrap: true,
      physics: const NeverScrollableScrollPhysics(),
      itemCount: languages.length,
      itemBuilder: (context, index) {
        final lang = languages[index];
        final isSelected = lang.code == currentLocale.languageCode;
        return ListTile(
          leading: Text(
            lang.flag,
            style: const TextStyle(fontSize: 24),
          ),
          title: Text(lang.name),
          trailing: Radio<String>(
            value: lang.code,
            groupValue: currentLocale.languageCode,
            onChanged: (_) => _selectLanguage(ref, lang.code),
          ),
          onTap: () => _selectLanguage(ref, lang.code),
          selected: isSelected,
        );
      },
    );
  }

  void _selectLanguage(WidgetRef ref, String code) {
    ref.read(localeNotifierProvider.notifier).setLocale(code, null);
    onSelected?.call();
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/features/i18n/presentation/
git commit -m "feat(i18n): add LanguageSelectorWidget"
```

---

### Task 8: Update app.dart

**Files:**
- Modify: `lib/app.dart`

- [ ] **Step 1: Update app.dart to use localeNotifierProvider**

Replace full content of `lib/app.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';

import 'core/constants/routes.dart';
import 'core/services/locale_service.dart';
import 'core/theme/app_theme.dart';
import 'features/i18n/application/locale_provider.dart';
import 'l10n/app_localizations.dart';

final _router = GoRouter(
  initialLocation: AppRoutes.splash,
  routes: [
    GoRoute(
      path: AppRoutes.splash,
      builder: (context, state) => const _SplashPage(),
    ),
  ],
);

class TokaApp extends ConsumerWidget {
  const TokaApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final locale = ref.watch(localeNotifierProvider);

    return MaterialApp.router(
      onGenerateTitle: (context) => AppLocalizations.of(context).appName,
      locale: locale,
      supportedLocales: LocaleService.supported,
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      theme: AppTheme.light,
      darkTheme: AppTheme.dark,
      themeMode: ThemeMode.system,
      routerConfig: _router,
    );
  }
}

class _SplashPage extends StatelessWidget {
  const _SplashPage();

  @override
  Widget build(BuildContext context) {
    // TODO(spec-03): replace with real onboarding/home redirect
    return const Scaffold(
      body: Center(
        child: Text('Toka', style: TextStyle(fontSize: 32)),
      ),
    );
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/app.dart
git commit -m "feat(i18n): connect LocaleNotifier to MaterialApp.router.locale"
```

---

### Task 9: Seed script

**Files:**
- Create: `scripts/seed_languages.ts`

- [ ] **Step 1: Create seed_languages.ts**

Create `scripts/seed_languages.ts`:

```typescript
import * as admin from 'firebase-admin';

// Run against the Firestore emulator:
// FIRESTORE_EMULATOR_HOST=localhost:8080 npx ts-node scripts/seed_languages.ts

process.env['FIRESTORE_EMULATOR_HOST'] = 'localhost:8080';
admin.initializeApp({ projectId: 'toka-dev' });

const db = admin.firestore();

const languages = [
  { code: 'es', name: 'Español', flag: '🇪🇸', arb_key: 'app_es', enabled: true, sort_order: 1 },
  { code: 'en', name: 'English', flag: '🇬🇧', arb_key: 'app_en', enabled: true, sort_order: 2 },
  { code: 'ro', name: 'Română', flag: '🇷🇴', arb_key: 'app_ro', enabled: true, sort_order: 3 },
];

async function seed() {
  for (const lang of languages) {
    await db.collection('languages').doc(lang.code).set(lang);
    console.log(`Seeded language: ${lang.code}`);
  }
  console.log('Done.');
  process.exit(0);
}

seed().catch((err) => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Commit**

```bash
git add scripts/
git commit -m "chore(i18n): add seed script for languages collection in emulator"
```

---

### Task 10: Integration test — language fetch

**Files:**
- Create: `test/integration/features/i18n/language_fetch_test.dart`

- [ ] **Step 1: Create language_fetch_test.dart**

Create `test/integration/features/i18n/language_fetch_test.dart`:

```dart
import 'package:fake_cloud_firestore/fake_cloud_firestore.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:toka/features/i18n/data/language_repository_impl.dart';

void main() {
  late FakeFirebaseFirestore fakeFirestore;
  late LanguageRepositoryImpl repo;

  setUp(() async {
    fakeFirestore = FakeFirebaseFirestore();
    repo = LanguageRepositoryImpl(firestore: fakeFirestore);

    // Populate languages (simulates the production Firestore state)
    final col = fakeFirestore.collection('languages');
    await col.doc('es').set({
      'code': 'es', 'name': 'Español', 'flag': '🇪🇸',
      'arb_key': 'app_es', 'enabled': true, 'sort_order': 1,
    });
    await col.doc('en').set({
      'code': 'en', 'name': 'English', 'flag': '🇬🇧',
      'arb_key': 'app_en', 'enabled': true, 'sort_order': 2,
    });
    await col.doc('ro').set({
      'code': 'ro', 'name': 'Română', 'flag': '🇷🇴',
      'arb_key': 'app_ro', 'enabled': true, 'sort_order': 3,
    });
  });

  test('fetches all enabled languages ordered by sort_order', () async {
    final langs = await repo.fetchAvailableLanguages();
    expect(langs.length, 3);
    expect(langs.map((l) => l.code).toList(), ['es', 'en', 'ro']);
  });

  test('collection is readable without authentication (no security rules in fake)', () async {
    // fake_cloud_firestore bypasses security rules — confirms the repo query shape is correct.
    final langs = await repo.fetchAvailableLanguages();
    expect(langs, isNotEmpty);
  });

  test('returns only enabled languages', () async {
    await fakeFirestore.collection('languages').doc('ro').update({'enabled': false});
    final langs = await repo.fetchAvailableLanguages();
    expect(langs.length, 2);
    expect(langs.any((l) => l.code == 'ro'), isFalse);
  });
}
```

- [ ] **Step 2: Run test**

```bash
flutter test test/integration/features/i18n/language_fetch_test.dart
```

Expected: All 3 tests PASS.

- [ ] **Step 3: Commit**

```bash
git add test/integration/features/i18n/
git commit -m "test(i18n): add integration tests for language fetch"
```

---

### Task 11: UI test — LanguageSelectorWidget

**Files:**
- Create: `test/ui/features/i18n/language_selector_widget_test.dart`

- [ ] **Step 1: Create widget test**

Create `test/ui/features/i18n/language_selector_widget_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:toka/features/i18n/application/language_provider.dart';
import 'package:toka/features/i18n/application/locale_provider.dart';
import 'package:toka/features/i18n/domain/language.dart';
import 'package:toka/features/i18n/domain/language_repository.dart';
import 'package:toka/features/i18n/presentation/language_selector_widget.dart';
import 'package:toka/l10n/app_localizations.dart';

final _testLanguages = [
  Language(code: 'es', name: 'Español', flag: '🇪🇸', arbKey: 'app_es', enabled: true, sortOrder: 1),
  Language(code: 'en', name: 'English', flag: '🇬🇧', arbKey: 'app_en', enabled: true, sortOrder: 2),
  Language(code: 'ro', name: 'Română', flag: '🇷🇴', arbKey: 'app_ro', enabled: true, sortOrder: 3),
];

class _FakeLanguageRepository implements LanguageRepository {
  @override
  Future<List<Language>> fetchAvailableLanguages() async => _testLanguages;
}

Widget _wrap(Widget child) {
  return ProviderScope(
    overrides: [
      languageRepositoryProvider.overrideWithValue(_FakeLanguageRepository()),
    ],
    child: MaterialApp(
      localizationsDelegates: const [
        AppLocalizations.delegate,
        GlobalMaterialLocalizations.delegate,
        GlobalWidgetsLocalizations.delegate,
        GlobalCupertinoLocalizations.delegate,
      ],
      supportedLocales: const [Locale('es'), Locale('en'), Locale('ro')],
      home: Scaffold(body: child),
    ),
  );
}

void main() {
  testWidgets('renders flag, name and radio button for each language', (tester) async {
    await tester.pumpWidget(_wrap(const LanguageSelectorWidget()));
    await tester.pump(); // trigger async provider

    expect(find.text('🇪🇸'), findsOneWidget);
    expect(find.text('Español'), findsOneWidget);
    expect(find.text('🇬🇧'), findsOneWidget);
    expect(find.text('English'), findsOneWidget);
    expect(find.text('🇷🇴'), findsOneWidget);
    expect(find.text('Română'), findsOneWidget);
    expect(find.byType(Radio<String>), findsNWidgets(3));
  });

  testWidgets('tapping a language calls setLocale on the notifier', (tester) async {
    String? selectedCode;

    await tester.pumpWidget(
      ProviderScope(
        overrides: [
          languageRepositoryProvider.overrideWithValue(_FakeLanguageRepository()),
        ],
        child: MaterialApp(
          localizationsDelegates: const [
            AppLocalizations.delegate,
            GlobalMaterialLocalizations.delegate,
            GlobalWidgetsLocalizations.delegate,
            GlobalCupertinoLocalizations.delegate,
          ],
          supportedLocales: const [Locale('es'), Locale('en'), Locale('ro')],
          home: Scaffold(
            body: LanguageSelectorWidget(
              onSelected: () { selectedCode = 'called'; },
            ),
          ),
        ),
      ),
    );
    await tester.pump();

    await tester.tap(find.text('English'));
    await tester.pump();

    expect(selectedCode, 'called');
  });

  testWidgets('golden: three languages displayed', (tester) async {
    await tester.pumpWidget(_wrap(const LanguageSelectorWidget()));
    await tester.pump();

    await expectLater(
      find.byType(LanguageSelectorWidget),
      matchesGoldenFile('goldens/language_selector_widget.png'),
    );
  });
}
```

- [ ] **Step 2: Create goldens directory and run with --update-goldens first**

```bash
mkdir -p test/ui/features/i18n/goldens
flutter test test/ui/features/i18n/language_selector_widget_test.dart --update-goldens
```

Expected: Golden file created at `test/ui/features/i18n/goldens/language_selector_widget.png`.

- [ ] **Step 3: Run widget tests normally**

```bash
flutter test test/ui/features/i18n/language_selector_widget_test.dart
```

Expected: All 3 tests PASS.

- [ ] **Step 4: Commit**

```bash
git add test/ui/features/i18n/
git commit -m "test(i18n): add widget tests and golden for LanguageSelectorWidget"
```

---

### Task 12: Run all tests

- [ ] **Step 1: Run all unit tests**

```bash
flutter test test/unit/
```

Expected: All tests PASS.

- [ ] **Step 2: Run all integration tests**

```bash
flutter test test/integration/
```

Expected: All tests PASS.

- [ ] **Step 3: Run all UI tests**

```bash
flutter test test/ui/
```

Expected: All tests PASS.

- [ ] **Step 4: Run flutter analyze**

```bash
flutter analyze
```

Expected: No issues.
